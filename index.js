import React from 'react';
import ReactDOM from 'react-dom/client';
import NetGenicStudio from './NetGenicStudio';
import './index.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <NetGenicStudio />
  </React.StrictMode>
);
